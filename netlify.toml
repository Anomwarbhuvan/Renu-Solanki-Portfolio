[build]
  command   = "npm run build"
  publish   = "_site"

[build.environment]
  NODE_VERSION = "18"

# Redirect /admin to /admin/ so Netlify Identity works correctly
[[redirects]]
  from   = "/admin"
  to     = "/admin/"
  status = 301
